<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.exe.paradox">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:theme">
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Transparent">

        </activity>
        <activity android:name=".HomeActivity" />

        <activity android:name=".MainActivity" />
        <activity android:name=".QuestionActivity" android:theme="@style/AppTheme1"/>
        <activity android:name=".LeaderboardActivity" />
        <activity android:name=".ReferActivity" android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LayoutBackCardFlip" />
        <activity android:name=".LayoutFrontCardFlip" />
        <activity android:name=".StatsActivity" android:theme="@style/AppTheme1"/>

        <activity android:name=".MembersActivity" android:theme="@style/AppTheme1"/>
        <activity android:name=".IntroductionActivity">
        </activity>
    </application>

</manifest>